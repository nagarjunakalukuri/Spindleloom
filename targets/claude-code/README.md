# Claude Code bundle (loose files)

Generated by `build_harness_artifacts.py` from the single source. Prefer the `claude-plugin` target (one `/plugin install`); use this only to hand-wire `.claude/`.

Copy `agents/` → `.claude/agents/`, `commands/` → `.claude/commands/`, `skills/` → `.claude/skills/`, `CLAUDE.md` → repo root, and `mcp/` + `.mcp.json` → repo root (MCP launches via `uv run` — install [uv](https://docs.astral.sh/uv/)). Wire `hooks/on_md_edit.py` as a PostToolUse Write|Edit hook in `.claude/settings.json` for the live spec gate.
