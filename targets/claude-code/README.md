# Claude Code bundle (loose files)

Generated by Shipwright from the single source. Prefer the `claude-plugin` target (one `/plugin install`); use this only to hand-wire `.claude/`.

Copy `agents/` → `.claude/agents/`, `CLAUDE.md` → repo root, and `mcp/` + `.mcp.json` → repo root for live traceability (launched via `uv run` — install [uv](https://docs.astral.sh/uv/), which provisions the MCP SDK on first run). Commands/skills/hooks live in the source — copy them too, or use the plugin.
