# GitHub Copilot bundle

Generated by Shipwright. Copy `.github/`, `.vscode/`, and `mcp/` into your repo root. Chat modes + `copilot-instructions.md` carry the fleet; `.vscode/mcp.json` registers the live traceability server, launched via `uv run` (install [uv](https://docs.astral.sh/uv/); it provisions the MCP SDK on first run).
