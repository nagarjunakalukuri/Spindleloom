# JUDGE VERDICT — e2e-medremind RUN 4 (post-fix: B1/B2/B3/B4/B5)
Judge: independent (not one of the fleet) · Date: 2026-07-10 · Basis: brief.md, 01–10, RTM.md, plus validate_reqs --strict / build_rtm --check / sloom flags tooling · Baseline: RUN 3 verdict (A−)

Fixes under test (from run 3 §4 "new binding constraints"): **B1** stem-aware input resolution (the `09-estimates.md` vs `09-estimation.md` silent edge drop); **B2** backlog-completeness / phantom-PBI check (`PBI-UNDEFINED`); **B3** flag re-work loop (surface *and* route); **B4** quality-ok gate (compound-shall must drop or be marker-signed, not prose-justified); **B5** ellipsis range lint (the `…` that evaded run 3's ASCII-only `..` regex).

## 1. Per-handoff verdict table (run 4 vs run 3)
| # | Agent | Received vs needed | Verdict | Run 3 | Evidence |
|---|---|---|---|---|---|
| 1 | doc-strategy-advisor | brief only; living-spec absent | CLEAN | CLEAN | Tier-1 Lean + regulated overlay; DOC-001..008 register with single owners; seeds RTM skeleton + Document Register; `FLAG(spec-steward)` on the missing living-spec. Same self-flagged gap as run 3 |
| 2 | brd-writer | brief + strategy; MRD-drop sanctioned | CLEAN | CLEAN | BR-OUTCOME/REFILL/QUEUE/SUB/CTRL/PHI/NFR atomic; `FLAG(mrd-writer)` makes the sanctioned skip auditable; opens OQ-1/2/3 + ASSUMPTION-1..5, each owned |
| 3 | urs-writer | brief + BRD + RTM | CLEAN | CLEAN | 15 URS rows, every Source cites a BR-/CON-; adds ASSUMPTION-6..9 (non-GAMP scope, verification depth, classification source, per-state); two FLAGs (security-reviewer, spec-steward) |
| 4 | prd-writer | brief + BRD + **URS** + RTM | CLEAN | CLEAN | URS routed and *used* — PRD-QUE-002/003 & PRD-SUB-002 cite URS-DAT-002/003 & URS-SAF-003; adds OQ-4/5/6 (cadence, TCPA consent, sub-UX) the upstream never owned |
| 5 | frd-writer | brief + PRD + URS + RTM | CLEAN | CLEAN | 32 atomic "system shall" rows w/ G-W-T; seeds 12 non-phantom PBIs; **adds FRD-CTRL-005 fail-closed** (genuine new safety behaviour) + send-window OQ-7 — richer, not thinner, than run 3 |
| 6 | srs-writer | brief + FRD + URS + **PRD (now routed)** | **CLEAN** | DEGRADED(minor) | **Run-3's minor cause healed:** PRD is a listed upstream (SR-PHI-002 ← PRD-PHI-001). All numerics are owned ASSUMPTION-15..19 w/ named ratifiers; one-obligation-per-row discipline stated and held. Caveat (not a downgrade): ASSUMPTION-15 mints a ≥99%/mo percentile the brief never stated — flagged for PM ratification, defensible (100% literal is untestable) |
| 7 | sdd-writer | brief + SRS + FRD + PRD + URS | CLEAN | CLEAN | FRD→SDD state model (RefillRequest received→pending→{approved,rejected,substitution-pending}→closed) traces cleanly; fail-closed classification diagram = FRD-CTRL-005; solution-recon/rfc absent, self-flagged low-risk (reuse-heavy). No phantom PBIs |
| 8 | backlog-manager | PRD+FRD+SRS+SDD+URS+RTM | **CLEAN** | DEGRADED(phantom) | **Run-3's phantom PBI-PLAT-004 defect is gone.** 13 PBIs, every one AC-bearing; mints exactly one justified enabler (PBI-ENABLE-001 ← SR-PHI-003, no FRD PBI carried it); DoR-blocks 4 PBIs on the absent DOC-004; **refuses to mint the spikes** as phantom, routes them instead |
| 9 | estimation-facilitator | backlog + brief + SDD/SRS ancestors | CLEAN | CLEAN | Re-estimates w/ Δseed; CTRL-001 ▲5→8 cascade cited to ASSUMPTION-8/20/23; PBI-CTRL-003 = `?` spike-gated; 56 pointed dev pts across 11 PBIs; routes the spike-split to backlog-manager rather than inventing a PBI ID |
| 10 | sprint-planner | **estimation (now resolved)** + backlog | **CLEAN** | DEGRADED(misroute) | **The run-3 silent edge drop is fixed.** Header ingests `09-estimation.md` — cites its exact 56 pts + 18–22 band, and *resolves the cascade decision estimation handed it* (option a stretch ~24 vs b re-rank → picks neither, holds ~20). Commits 16 conditional pts; no 3-vs-5 contradiction; refuses phantom spike IDs; will not fake-Ready DoR-blocked stories |

**Tally: 10 CLEAN / 0 DEGRADED / 0 BROKEN (run 3: 7 / 3 / 0).** Net +3 upgrades (srs — PRD routed; backlog — no phantom; sprint — edge resolved), 0 downgrades.

## 2. Cross-artifact contradiction hunt — re-checking run 3's three A-blockers

### (a) estimation→sprint edge & sprint-count agreement — HEALED (was run-3's sharpest live contradiction)
Run 3: sprint-planner keyed the edge on `09-estimates.md`, the file was `09-estimation.md`, the handoff dropped silently, and the plan re-derived blind → a live 3-vs-5-sprint contradiction. Run 4: `10-sprint1-plan.md` names `09-estimation.md` in its upstream line and consumes its numbers verbatim — **56 pointed dev pts, 18–22 velocity band, PBI-SUB-001=3 anchor** — and its §"Cascade decision" quotes estimation's two offered options and adjudicates them. That is proof of ingestion, not coincidence. **Numbers agree end-to-end:** estimation 56÷~20 ≈ 2.8 → "~3 sprints, achievable but at risk"; sprint-planner ~20 capacity, funnel closes Sprint 3, CON-5 "at risk"; both put PBI-CTRL-003 at "Sprint 3–4." Arithmetic verified: Sprint-1 commit 5+8+3 = **16 pts** = stated; pointed total 5+8+3+8+5+5+8+3+3+5+3 = **56** = stated. **No 3-vs-5 contradiction survives.** Adversarial note: both docs accept that CTRL-003 (the *manual* controlled-substance path) may land in a 4th sprint — but the automation-*exclusion* gates (PBI-CTRL-001/002) are in Sprint 1, so the 3-sprint slice ships with controlled substances safely excluded and handled on the existing manual process. That is a coherent, compliant slice, not a dropped requirement.

### (b) phantom PBI — GONE (`validate_reqs --strict` clean; PBI-UNDEFINED empty)
`validate_reqs.py … --strict` **exits 0** (149 Req-IDs); the advisory pass reports **zero** QUALITY / RANGE-SHORTHAND / PBI-UNDEFINED findings. The B2 check is real: `backlog_completeness_lint` collects every `PBI-…` token as *referenced* and counts a PBI *defined* only when it is the subject of a table row carrying Given/When/Then — exactly the run-3 hole where PBI-PLAT-004 had an RTM row but no AC row. All 13 PBIs (ENABLE, PHI, CTRL-001/002/003, RMD-001/002, RFL-001, QUE-001/002/003, SUB-001/002) have AC-bearing defining rows in `08-backlog.md`; FRD seeds 12, backlog mints ENABLE. The two discovery spikes are deliberately left **un-IDed** — `frd`/`estimation`/`sprint`/RTM all state the same reasoning ("assigning a PBI ID would be a phantom PBI"), so they never enter the *referenced* set. The lint would fire on a real phantom; it stays silent because there is none.

### (c) flag loop — PRESENT and now ROUTING (but still does not close within-run)
`sloom flags` surfaces **12 open flags across 8 owners** as a re-work queue. Unlike run 3's mostly gap-declaration flags, these now carry **actionable forward routing**: estimation `FLAG(backlog-manager)` to split a spike out of PBI-CTRL-003, and sprint-planner `FLAG(backlog-manager)` to mint *both* discovery spikes as AC-bearing PBIs. That is the loop being used to dispatch re-work, not just to log absence. **What it still cannot do is close:** backlog-manager (08) runs upstream of the agents (09, 10) that raise the spike-mint requests, so the spikes cannot be minted, PBI-CTRL-003 cannot be pointed, and DOC-004 cannot be authored (no security-reviewer in this 10-doc chain) within a single linear pass. Feedback is visible, owned, and routed — closure remains cross-pass/manual.

## 3. Fix verdicts
| Fix | Verdict | Strongest evidence |
|---|---|---|
| **B1** stem-aware input resolution | **FIXED** | The richest downstream handoff that silently dropped in run 3 now resolves: sprint-planner ingests `09-estimation.md` and re-uses its 56-pt / 18–22 numbers and its cascade question. The 3-vs-5 contradiction that B1 was meant to prevent does not recur (§2a) |
| **B2** backlog-completeness / phantom check | **FIXED** | `backlog_completeness_lint` emits `PBI-UNDEFINED` for any PBI referenced-but-not-AC-defined; `--strict` exits 0 with zero findings; all 13 PBIs carry AC rows; spikes correctly left un-IDed so they cannot become phantoms. The exact run-3 machine-invisible defect is now machine-caught |
| **B3** flag re-work loop | **PARTIAL** | The queue is real and, unlike run 3, **routes** re-work with named next actions (mint the two spikes; author DOC-004). But it does not *close* within the run — the flagged owner (backlog-manager) ran before the flaggers, so CTRL-003 stays `?`, the spikes stay un-minted, DOC-004 stays absent. Surfaced + routed, not resolved |
| **B4** quality-ok gate | **FIXED (the strongest fix)** | Run 3's A4 was *routed around* (density flat at ~38, prose-justified). Run 4: `--strict` exits **0**, advisory pass reports **zero** compound-shall/vague findings, and there is exactly **one** `quality-ok:` marker in the whole run (BR-QUEUE-002). The clean pass is achieved by genuinely atomic "system shall [one obligation]" authoring, **not** by mass marker-suppression and **not** by prose (the code closed that escape hatch). Honest caveats: the lone marker sits on a "the business requires that…" line the `shall`-regex could never fire on (cargo-cult, harmless); and the lint inspects only the shall-clause, so compound logic that legitimately moved into Given/When/Then AC cells is out of its view — correct requirements engineering, but it means the metric measures shall-statement atomicity, not total AC conjunction count |
| **B5** ellipsis range lint | **FIXED (with a narrowness note)** | `RANGE_SHORTHAND` now matches `(?:\.\.\.?|…)` — the `…` that evaded run 3 is caught; advisory pass reports zero. Residual gap: the regex still requires a **three-part** Req-ID, so `02-brd.md:71` "ASSUMPTION-1 … ASSUMPTION-5" (a two-part ID) evades it. Harmless here — all five ASSUMPTIONs are individually defined and ASSUMPTION-N is not a traceability Req-ID — but it marks the lint's coverage boundary |

## 4. Overall grade: **A** (run 3: A−; delta +1)

All three run-3 A-blockers are verifiably closed with tool backing, not narrative: the estimation→sprint edge **resolves and its numbers agree** (no 3-vs-5); the phantom-PBI class is both **absent and now machine-caught** (`--strict` exit 0, `PBI-UNDEFINED` empty); and the flag register **routes** re-work with owners. B4 — the fix run 3 was routed around — is the standout: compound-shall density genuinely collapsed to zero-flagged through atomic authoring, verified by a strict gate that exits 0 with a single, unnecessary marker rather than a wall of suppressions. 10/10 handoffs CLEAN, 0 BROKEN, up from 7/3/0, with three real upgrades and no downgrades.

Held at **A rather than A+** for three honest reasons, none a defect: **(1)** B3 only half-lands — feedback is now routed but the run ends with live backward dependencies it structurally cannot resolve (spikes un-minted, PBI-CTRL-003 un-pointed, DOC-004 un-authored), so its headline deliverable, a committable Sprint 1, is explicitly *conditional*; **(2)** part of the CLEAN sweep rides on a leaner, better-factored backlog (56 pts / 13 PBIs vs run 3's 106 / more, with audit/observability/encryption correctly folded into functional PBIs' DoD instead of horizontal-slice PBIs) — good practice that also shrinks the contradiction surface, worth naming; **(3)** the two lint passes read clean partly at their own boundaries (B4 scans only the shall-clause; B5 only three-part IDs). This is an honest A: adversarial re-checking refuted none of the CLEAN verdicts.

### NEW binding constraint the fixes exposed (the run-N pattern)
**The pipeline is single-pass / non-re-entrant, and the run-4 fixes made the fleet honest enough to expose it.** The run-2→3→4 pattern held again — each repaired mechanism promotes the next hidden one to binding. Run 3's binding constraint was *silent data loss at the last unrouted edge* (the filename mismatch). Run 4 fixed routing/resolution, phantom-detection, and DoR honesty — and precisely *because* the fleet now (a) refuses to mint phantom spikes and (b) refuses to fake-Ready DoR-blocked stories, it surfaces that genuine completion requires a **second orchestration pass** the linear run cannot perform: loop backlog-manager to define the two spikes → re-estimate PBI-CTRL-003 → and author DOC-004 (which needs a `security-reviewer` the 10-agent chain never invokes). The constraint has moved from *"the fleet loses information silently"* (run 3) to *"the fleet correctly identifies re-work it has no mechanism to execute"* (run 4). Loop-closure — re-entrancy / a second planning pass, and pulling `security-reviewer` into the chain for regulated features — is the next binding thing to mechanize.
